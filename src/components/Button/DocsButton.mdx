import { Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Button } from './Button';

# Buttons

## Imports

```
import { Button } from "my-react-libary-sb";
```

### Button types

Use the `type` prop to change the visual style of the `Button`. You can set the value to `primary` or `secondary`.

<Canvas withSource="open">
  <Story id="components-button--default" />
  <Story id="components-button--primary" />
  <Story id="components-button--secondary" />
</Canvas>

### Props

<ArgsTable of={Button} />